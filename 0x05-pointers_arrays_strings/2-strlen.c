#include "main.h"
#include <string.h>

/**
 * _strlen - Check the length of a string
 * @s: variable.
 *
 *Return: The Lenght of the string
 */
 int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{
continue;
}
return (i);
}
