#include "main.h"
#include <string.h>
/**
  * rev_string - reverses a string
  * @s: Variable
  * Return: No return
  */

void rev_string(char *s)
{
int i;
int j = length(s[i])
for (i = j; i > 0; i--)
{
_putchar(s[i]);
}
}
